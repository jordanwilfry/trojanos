/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "pexels.com",
            "localhost",
            "firebasestorage.googleapis.com",
            "placehold.co"
        ],
    }
};

export default nextConfig;
